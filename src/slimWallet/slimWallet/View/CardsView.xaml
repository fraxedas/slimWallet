<?xml version="1.0" encoding="utf-8"?>

<base:BaseView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:base="clr-namespace:slimWallet.Base;assembly=slimWallet"
    x:Class="slimWallet.CardsView"
    Title="SlimWallet">

    <ContentPage.ToolbarItems>

        <ToolbarItem Icon="add.png"
            Command="{Binding AddCommand}" />

    </ContentPage.ToolbarItems>

    <ContentPage.Content>

        <ListView
            ItemsSource="{Binding Model.List}"
            SelectionMode="None"
            IsPullToRefreshEnabled="false"
            ItemTapped="Handle_ItemTapped"
            RowHeight="100"
            SeparatorVisibility="None">
       

            <ListView.ItemTemplate>

                <DataTemplate>

                    <ImageCell
                        Text = "{Binding Name}"
                        ImageSource="{Binding FrontImage, Converter={StaticResource ImageSource}}" />

                </DataTemplate>

            </ListView.ItemTemplate>

        </ListView>

    </ContentPage.Content>

</base:BaseView>
